<Window x:Class="GRGlibHF.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
		xmlns:cm="clr-namespace:SnappyWinscard;assembly=SnappyWinscard"
        mc:Ignorable="d"
        Title="GRG Library HF Card Reader" SizeToContent="WidthAndHeight" ResizeMode="NoResize"
		d:DataContext="{d:DesignInstance cm:CardIo}"
		>
    <Window.Resources>
        <Style x:Key="FocusVisual">
            <Setter Property="Control.Template">
                <Setter.Value>
                    <ControlTemplate>
                        <Rectangle Margin="2" SnapsToDevicePixels="true" Stroke="{DynamicResource {x:Static SystemColors.ControlTextBrushKey}}" StrokeThickness="1" StrokeDashArray="1 2"/>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <SolidColorBrush x:Key="Button.Static.Background" Color="#FFF1EFEF"/>
        <SolidColorBrush x:Key="Button.Static.Border" Color="#FF707070"/>
        <SolidColorBrush x:Key="Button.MouseOver.Background" Color="#FFBEE6FD"/>
        <SolidColorBrush x:Key="Button.MouseOver.Border" Color="#FF3C7FB1"/>
        <SolidColorBrush x:Key="Button.Pressed.Background" Color="#FFC4E5F6"/>
        <SolidColorBrush x:Key="Button.Pressed.Border" Color="#FF2C628B"/>
        <SolidColorBrush x:Key="Button.Disabled.Background" Color="#FFF4F4F4"/>
        <SolidColorBrush x:Key="Button.Disabled.Border" Color="#FFADB2B5"/>
        <SolidColorBrush x:Key="Button.Disabled.Foreground" Color="#FF838383"/>
        <Style TargetType="{x:Type ToggleButton}" BasedOn="{StaticResource {x:Type ToggleButton}}">
            <Setter Property="Background" Value="{StaticResource Button.Static.Background}"/>
        </Style>
    </Window.Resources>
    <Grid Width="400">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="51*"/>
            <ColumnDefinition Width="349*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <!-- Banner row -->
            <RowDefinition Height="*"/>
            <!-- Content row -->
        </Grid.RowDefinitions>

        <!-- Banner Image at the top -->
        <Image Source="/kcwbanner.png" Height="62" Grid.Row="0" HorizontalAlignment="Left" Margin="0,10,0,10" Grid.ColumnSpan="2"/>

        <!-- Main content below the banner -->
        <Grid Grid.Row="1" HorizontalAlignment="Left" Width="496" Height="59" Margin="10,0,0,10" Grid.ColumnSpan="2">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="40"/>
                <RowDefinition Height="40"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" MinWidth="166"/>
                <ColumnDefinition Width="Auto" MinWidth="0"/>
                <ColumnDefinition Width="160"/>
                <ColumnDefinition Width="65.333"/>
            </Grid.ColumnDefinitions>

            <Button x:Name="buttonGetUid" Content="Get card UID" Click="ButtonGetUid_Click" Margin="6,6,6,6"/>
            <ComboBox x:Name="comboboxDevice" Grid.Column="1" Margin="5,5,5,5" Grid.ColumnSpan="3" ItemsSource="{Binding Devices}" SelectedItem="{Binding CurrentDevice}" />
            <TextBlock HorizontalAlignment="Right" Margin="0,5,5,0" Grid.Row="1" TextWrapping="Wrap" Text="Status" VerticalAlignment="Top" Grid.RowSpan="5" Width="32" d:LayoutOverrides="LeftMargin, RightMargin, TopMargin, BottomMargin"/>
            <Border BorderBrush="{DynamicResource {x:Static SystemColors.InfoTextBrushKey}}" BorderThickness="1" Margin="5,5,5,5" Padding="3" Grid.Row="1" Grid.Column="1" Grid.ColumnSpan="3">
                <TextBlock x:Name="textBlockStatus" TextWrapping="Wrap" Background="{DynamicResource {x:Static SystemColors.InfoBrushKey}}"/>
            </Border>
        </Grid>
    </Grid>
</Window>